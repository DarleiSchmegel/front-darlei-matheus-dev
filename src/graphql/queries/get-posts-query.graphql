query GetAllPosts {
  posts(orderBy: date_DESC, stage: PUBLISHED) {
    id
    slug
    tags
    title
    date
    excerpt
    stage
    author {
      name
      id
      title
      biography
      picture {
        url
      }
    }
    coverImage {
      url
    }
  }
}
